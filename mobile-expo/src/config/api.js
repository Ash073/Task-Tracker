// Change this to your backend server IP address
// Use your computer's local IP, NOT localhost (mobile can't reach localhost)
// Find your IP: Windows → ipconfig | Mac/Linux → ifconfig
const API_BASE_URL = 'https://task-tracker-cm69.onrender.com/api';

export default API_BASE_URL;
